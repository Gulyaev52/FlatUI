mixin button({ text, size="small", type="default", theme="default" })
    -const buttonModifiers = [
    -    `button_size_${size}`,
    -    `button_type_${type}`,
    -    `button_theme_${theme}`
    -]
    button.button(class = buttonModifiers)&attributes(attributes)= text